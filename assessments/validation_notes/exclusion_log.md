# Exclusion Log

## SHELTER_WASH_FSS_IDP_2025_07

Title: Shelter-WASH-FSS IDP Assessment Results

Current status: `restricted_pending_geography`

Current validator role: `excluded_or_restricted`

Reason for restriction:

The project notes classify this source as unresolved for the current Gaza assessment scope. It should not support project findings until the relevant geography is confirmed in the document.

Allowed use at current status:

- Audit inventory only.
- No support for analytic findings.

Promotion requirements:

- Confirm the relevant geography in the source document.
- Record exact section, page, table, or figure.
- Update `manifest.csv`.
- Add a specific permitted manuscript role.
- Add the source to `citation_audit/source_claim_matrix.csv` only after scope is resolved.

## General exclusion triggers

A source is excluded or downgraded when:

- geography is outside project scope or ambiguous;
- reporting date is unclear and the claim requires temporal precision;
- source organization cannot be identified;
- a synthesis source is being used as field validation;
- mechanism support is being used for prevalence language;
- a derived source has not been checked against its original source.
