# EOD Notes — 2026-05-23

## Repository

`pskeffington/Gaza-WASH`

## Work completed

- Confirmed repository target and current main-branch baseline.
- Established that all project work should be conducted as repo-native files rather than detached ZIP packages.
- Reviewed the existing repository structure and confirmed the current README frames the project as a reproducible Gaza WASH public-health intelligence workspace.
- Classified assessment materials by validator role for manuscript use.
- Defined a controlled validation vocabulary for source and claim auditing.
- Drafted the assessment workflow structure for future repo integration.

## Validator posture

Primary household WASH validator:

- `2025-08 Gaza Joint WASH Assessment Report`

Primary local hotspot validators:

- `Enum Area 32 — Al Mawasi 7`
- `Enum Area 8 — As Sawarah`

Indicative trend validators:

- `2025-04 WASH Light-Touch Monitoring Round 3`
- `2025-06 WASH Light-Touch Monitoring Round 4`

Infrastructure and recovery validator:

- `Gaza IRDNA / RDNA Final, February 2025`

Background-only validator:

- `Health implications of limited access to safe water in Gaza`

Excluded or restricted-use material:

- `Shelter-WASH-FSS IDP Assessment Results, July 2025` should not be used for Gaza claims unless a Gaza-specific section is confirmed. Current reviewed content appears West Bank-oriented.

## Manuscript rules set

- Do not use secondary-extracted water-quality indicators as high-weight manuscript evidence until confirmed against primary or official validators.
- Use Light-Touch Monitoring only as indicative trend evidence, not as full-population prevalence unless the sentence is explicitly limited to assessed households or key informants.
- Use enumeration-area factsheets for local validation only; do not generalize them Gaza-wide.
- Use Gaza RDNA for infrastructure damage, service disruption, and reconstruction context; do not use it as a household LPPD outcome survey.
- Use background water-quality synthesis only for mechanism, not current 2025 field validation.

## Proposed repo objects

```text
assessments/
  README.md
  manifest.csv
  schema/
    validation_schema.json
  source_documents/
  extracted_tables/
  gis_layers/
  figures/
  validation_notes/
    primary_validator_audit.md
    manuscript_claim_rules.md
    exclusion_log.md
    workflow_validation_report.md
  citation_audit/
    source_claim_matrix.csv
    official_source_trace.csv
scripts/
  validate_assessments.py
.github/workflows/
  validate_assessments.yml
```

## Validation workflow target

The planned validator should check:

- Required assessment directories exist.
- `manifest.csv` has required columns.
- `validation_status` uses controlled vocabulary.
- `original_or_derived` uses controlled vocabulary.
- Manifest paths remain under `assessments/`.
- Duplicate file names and paths are flagged.
- Claim-source mappings match manifest entries.
- High-weight claims do not rely on background-only or excluded sources.
- Excluded-geography sources appear in the exclusion log.
- Strict mode confirms file paths exist when source documents are present locally.

## Current blocker

Direct GitHub write operations through the connector were blocked by the tool safety gate earlier in the session. Repo read access worked. If write access remains blocked, apply repo changes locally and push from the local clone.

## Next action

Create branch:

```bash
git checkout -b assessment-validator-workflow
```

Add the assessment validator files under the repo paths listed above.

Run local validation:

```bash
python scripts/validate_assessments.py --metadata-only
```

Commit:

```bash
git add assessments scripts .github/workflows docs/eod_notes_2026-05-23.md
git commit -m "Add Gaza WASH EOD assessment notes"
```
